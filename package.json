{
	"name": "template-neon-db-mcp",
	"version": "1.0.0",
	"description": "Template voor een remote MCP-server op Cloudflare Workers met Microsoft Entra ID (Azure AD) OAuth, per rol en per tabel instelbare rechten, en een Neon Postgres-database",
	"private": true,
	"type": "module",
	"scripts": {
		"dev": "wrangler dev",
		"deploy": "wrangler deploy",
		"type-check": "tsc --noEmit",
		"cf-typegen": "wrangler types",
		"test": "vitest run",
		"test:watch": "vitest"
	},
	"dependencies": {
		"@cloudflare/workers-oauth-provider": "^0.8.1",
		"@modelcontextprotocol/sdk": "1.29.0",
		"@neondatabase/serverless": "^1.1.0",
		"agents": "^0.17.1",
		"hono": "^4.12.0",
		"node-sql-parser": "^5.4.0",
		"zod": "^4.4.3"
	},
	"devDependencies": {
		"@cloudflare/workers-types": "^4.20260424.1",
		"typescript": "^5.8.3",
		"vitest": "^4.1.11",
		"wrangler": "~4.105.0"
	}
}
